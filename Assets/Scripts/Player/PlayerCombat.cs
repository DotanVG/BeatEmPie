using UnityEngine;

/// <summary>
/// Handles Shushki's pie-throwing combat actions.
/// Manages pie selection, throwing, cooldowns, and combo tracking.
/// </summary>
public class PlayerCombat : MonoBehaviour
{
    // Currently selected pie type (enum PieType)
    // Reference to PieInventory
    // Reference to Animator

    // Update: listen for attack input

    // ThrowPie: instantiate selected pie prefab upward/toward target

    // SwitchPie: cycle through available pie types
}
